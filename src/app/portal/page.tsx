import Portal from "@/components/Portal";

export const metadata = {
  title: "Client Portal — Digital Defensive",
  description:
    "Sign in to view your findings, security metrics, reports, and plan details.",
};

export default function PortalPage() {
  return <Portal />;
}
